package com.vladlen.dotaautochessguide.screens.guides.di

import com.vladlen.dotaautochessguide.application.di.holder.BaseComponentHolder

class GuidesComponentHolder : BaseComponentHolder<GuidesComponent>() {

    companion object {
        val instance = GuidesComponentHolder()
    }
}