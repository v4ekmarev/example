package com.vladlen.dotaautochessguide.screens.statistics.di

import com.vladlen.dotaautochessguide.application.di.holder.BaseComponentHolder

class StatisticsComponentHolder : BaseComponentHolder<StatisticsComponent>() {

    companion object {
        val instance = StatisticsComponentHolder()
    }
}