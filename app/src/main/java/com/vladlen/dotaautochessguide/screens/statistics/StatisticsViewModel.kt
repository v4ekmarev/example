package com.vladlen.dotaautochessguide.screens.statistics

import android.arch.lifecycle.ViewModel

class StatisticsViewModel : ViewModel(), StatisticsContract {

    override fun onCleared() {
        super.onCleared()
    }
}