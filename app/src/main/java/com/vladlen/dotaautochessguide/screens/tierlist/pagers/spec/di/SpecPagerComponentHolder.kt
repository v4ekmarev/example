package com.vladlen.dotaautochessguide.screens.tierlist.pagers.spec.di

import com.vladlen.dotaautochessguide.application.di.holder.BaseComponentHolder

class SpecPagerComponentHolder : BaseComponentHolder<SpecPagerComponent>() {

    companion object {
        val instance = SpecPagerComponentHolder()
    }
}