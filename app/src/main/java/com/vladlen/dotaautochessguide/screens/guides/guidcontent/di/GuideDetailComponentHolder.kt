package com.vladlen.dotaautochessguide.screens.guides.guidcontent.di

import com.vladlen.dotaautochessguide.application.di.holder.BaseComponentHolder

class GuideDetailComponentHolder : BaseComponentHolder<GuideDetailComponent>() {

    companion object {
        val instance = GuideDetailComponentHolder()
    }
}